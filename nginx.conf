server {
  listen 80;
  server_name _;

  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;  # Always serve index.html for unknown routes
    index index.html;
  }

  # Add other configuration if necessary, e.g., caching, error pages, etc.
}
